# AutoCMEX — UI 设计规范

## 全局布局

### 主窗口布局

项目主窗口采用 **左右两栏** 布局，左窄右宽，窗口可自由缩放：

```
┌──────────────────────────────────────────┐
│  ┌──────────┐  ┌──────────────────────┐  │
│  │          │  │                      │  │
│  │  左栏    │  │       右栏           │  │
│  │  (窄)   │  │       (宽)           │  │
│  │          │  │                      │  │
│  │          │  │                      │  │
│  └──────────┘  └──────────────────────┘  │
└──────────────────────────────────────────┘
```

### 左栏布局

左栏用于切换功能板块，从上到下依次为：

| 位置 | 内容 |
|------|------|
| 顶端 | 项目 Logo（暂用占位图标） |
| Logo 下方 | 功能板块按钮：整合、猜测、信息 |
| 底端上方 | 功能板块按钮：设置、帮助 |
| 最底端 | 版本号 |

```
┌──────────┐
│  Logo    │  ← 顶端
│──────────│
│  整合    │
│  猜测    │  ← 主要功能板块
│  信息    │
│          │
│          │  ← 弹性空白区域
│          │
│──────────│
│  设置    │
│  帮助    │  ← 辅助功能板块
│──────────│
│  v1.0.0  │  ← 版本号（最底端）
└──────────┘
```

### 右栏布局

右栏展示当前选中功能板块的内容。所有板块场景在启动时全部预加载，切换时通过显隐控制，各板块维护独立的 `.tscn` 文件。

---

## 各板块 UI 布局

### 整合板块

暂不开发，UI 待定。

### 猜测板块

猜测板块右栏顶部有 Boss 下拉选择框（默认选中第一个 Boss），下方分为三区域：

```
┌──────────────────────────────────────────┐
│  Boss: [下拉选择框                   ▼]  │
├──────────────────────────────────────────┤
│  ┌─────────────────┐ ┌────────────────┐  │
│  │  符卡—创作者    │ │                │  │
│  │  对应表         │ │  猜测文本      │  │
│  │  (Tree 控件)    │ │  输入框        │  │
│  │                 │ │                │  │
│  │  [按钮区]       │ │  [模糊化处理]  │  │
│  ├─────────────────┤ ├────────────────┤  │
│  │  创作者别名表   │ │  回应显示栏    │  │
│  │  (表状结构)     │ │                │  │
│  │                 │ │                │  │
│  │  [按钮区]       │ │                │  │
│  └─────────────────┘ └────────────────┘  │
└──────────────────────────────────────────┘
```

| 区域 | 位置 | 内容 |
|------|------|------|
| Boss 选择 | 顶部 | 下拉框，切换当前被猜测的 Boss |
| 左上 | 左侧上方 | 符卡—创作者对应表（Tree 控件，仅显示当前 Boss） |
| 左下 | 左侧下方 | 创作者别名表 |
| 右 | 右侧 | 上方为猜测文本输入框 + 模糊化处理按钮，下方为回应显示栏 |

- 左上和左下之间为可拖拽分割线（VSplitContainer）。
- 功能按钮（导入、导出、添加、删除）放置在对应栏目旁。
- 从工程文件导入按钮预留但禁用，hover 提示"功能开发中"。
- 未配置 AI 模型时，模糊化处理按钮禁用，hover 提示"请先配置 AI 模型"。

### 信息板块

暂不开发，UI 待定。

### 设置板块

设置板块右栏分为三区域：

```
┌──────────────────────────────────────────┐
│  ┌────────────────────────────────────┐  │
│  │           搜索栏                   │  │
│  ├────────────┬───────────────────────┤  │
│  │  配置类别  │  具体配置项           │  │
│  │            │                       │  │
│  │  · AI模型  │  ┌─────────────────┐  │  │
│  │  · 群聊    │  │  API 格式: ...  │  │  │
│  │  · 猜测    │  │  请求地址: ...  │  │  │
│  │  · 整合    │  │  模型 ID: ...   │  │  │
│  │  · 信息    │  │  API 密钥: **** │  │  │
│  │  · 帮助    │  │  [测试连接]     │  │  │
│  │  · 通用    │  └─────────────────┘  │  │
│  └────────────┴───────────────────────┘  │
└──────────────────────────────────────────┘
```

| 区域 | 位置 | 内容 |
|------|------|------|
| 搜索栏 | 顶部，横跨左右 | 用于搜索配置项 |
| 左栏 | 左侧 | 配置类别列表（按板块 + 全局分类） |
| 右栏 | 右侧 | 当前类别下的具体配置项 |

AI 模型配置项包含"测试连接"按钮，点击后验证 API 连通性。

### 帮助板块

内置 Markdown 渲染区域，显示项目帮助文档。

---

## 设计原则

1. **左窄右宽**：左栏仅用于导航，右栏为主要工作区。
2. **板块隔离**：切换板块时右栏内容完全替换，各板块 UI 独立。
3. **一致性**：同类操作的按钮风格保持一致。
4. **响应式**：窗口缩放时右栏自适应，左栏保持固定宽度。
5. **状态反馈**：不可用功能以禁用态展示并给出 hover 提示，导入失败弹窗告知具体错误。
